; A standard's heading and prose are the durable half and read as text; the
; verification lines are the half that moves, and a reader skimming for what
; changed is looking for those numbers.

(section_marker) @punctuation.special
(standard_name) @markup.heading

(verification_key) @keyword
(header_key) @variable.other.member
(value) @string

(comment) @comment
